<h3>Добавление нового мема</h3>
<%= form_for :meme, url: memes_path, html: {multipart: true } do |f| %>
  <% if @meme.errors.any? %>
    <% @meme.errors.full_messages.each do |msg| %>
      <div class="alert alert-danger"><%= msg %></div>
    <% end %>
  <%end %>
  <p>
    Заголовок<br>
    <%= f.text_field(:title, {:class => 'form-control'}) %>
  </p>
  <p>
    Путь к картинке<br>
    <%= f.file_field :image  %>
  </p>
  <p>
    Пояснение<br>
    <%= f.text_area(:body, {:class => 'form-control'}) %>
  </p>
  <p>
    <%= f.submit({:class => 'btn btn-success'}) %>
  </p>
<% end %>